/** Narration for the shore — one sentence at a time, placed on thirds. */
import { story } from "./story";
export { story } from "./story";
export type { BeachLine } from "@/js/gsap/beachTimeline";

export const beachLines = story.beach.lines;

/** Whispered lines behind the hidden flower and star. */
export const hiddenQuotes = [
  "thank you for being here.",
  "some people are just good weather.",
  "this page exists because you do.",
];
